import React from 'react'

export default function List() {
    return (
        <div>
            对对对
            <button onClick={ () => {
                alert(0)
            } }>button</button>
        </div>
    )
}
